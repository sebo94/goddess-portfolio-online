import React from "react";
import classes from "./InstagramFeed.module.css";

const instagramFeed = (props) => (
  <div className={classes.InstagramFeed}>
    <h1>I am your instagram feed</h1>
  </div>
);

export default instagramFeed;
